package com.event.booking.userservice.exception.enums;

public enum ExceptionCode {
    USER_NOT_FOUND,
    INVALID_CREDENTIALS,
    INVALID_ROLE,
    INVALID_TOKEN,
    USER_ALREADY_EXISTS,
    INTERNAL_SERVICE_ERROR;
}
